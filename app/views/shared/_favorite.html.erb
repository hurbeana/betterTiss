<% if current_user %>
  <% if favorite?(favobj) %>
    <%= link_to "unfavorite", "#{path}?tiss_id=#{favobj.tiss_id}&type=unfavorite", method: :put %>
  <% else %>
    <%= link_to "favorite", "#{path}?tiss_id=#{favobj.tiss_id}&type=favorite", method: :put %>
  <% end %>
<% end %>
