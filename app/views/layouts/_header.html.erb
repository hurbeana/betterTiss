<header>

    <ul class="sidenav">
      <li><%= content_tag :a, "BetterTiss", href: root_path, id: "logo" %></li>

      <!--li><%= content_tag :a, "Home", href: root_path, class: "button" %></li-->

      <% if logged_in? %>
        <li class="dropdown" style="float: right;">
          <%= content_tag :a, "PROFILE NAME", href: root_path, class: "dropbtn button" %>
          <div class="dropdown-content" style="right: 0">
            <%= content_tag :a, "Profile", href: current_user, class: "button" %>
            <%= content_tag :a, "Settings", href: edit_user_path(current_user), class: "button" %>
            <%= content_tag :a, "Log out", href: logout_path, method: :delete, class: "button" %>
          </div>
        </li>
        <!-- li><%= content_tag :a, "Profile", href: current_user, class: "button" %></li>
      <li><%= content_tag :a, "Settings", href: edit_user_path(current_user), class: "button" %></li>
      <li><%= content_tag :a, "Log out", href: logout_path, method: :delete, class: "button" %></li -->

      <% else %>
        <li style="float: right"><%= content_tag :a, "Login", href: login_path, class: "button" %></li>
        <li style="float: right"><%= content_tag :a, "Sign Up", href: signup_path, class: "button" %></li>

      <% end %>

    </ul>


  <!--div class="container">
    <%= link_to "sample app", root_path, id: "logo" %>
    <nav>
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to "Home", root_path %></li>
        <li><%= link_to "Help", help_path %></li>
        <% if logged_in? %>
          <li><%= link_to "Users", users_path %></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              Account <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "People Search", people_search_path %></li>
              <li class="divider"></li>
              <li><%= link_to "Profile", current_user %></li>
              <li><%= link_to "Settings", edit_user_path(current_user) %></li>
              <li class="divider"></li>
              <li>
                <%= link_to "Log out", logout_path, method: :delete %>
              </li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to "Log in", login_path %></li>
        <% end %>
      </ul>
    </nav>
  </div-->

</header>

<div class="stickyHeader"></div>